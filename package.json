{
  "name": "html-viewer",
  "version": "1.0.0",
  "description": "Vellum — local HTML file viewer",
  "private": true,
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "start": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "vite": "^7.1.5"
  }
}
